import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  box: {
    height: 150,
    width: "50%",
    backgroundColor: "#000",
  },
});

export default styles;
